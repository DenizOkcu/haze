Implement the `csv-query.js` CLI in `/app` according to `/app/SPEC.md`.

Rules:
- Work only in `/app` and write your implementation into `/app/csv-query.js`.
- Do not modify `data.csv`, `SPEC.md`, or create other files unless you need
  scratch files (they are ignored; only `csv-query.js` is graded).
- No npm dependencies; Node.js standard library only.
- You can sanity-check your implementation against the example in `SPEC.md`
  using the provided `/app/data.csv`.

When you are done, reply with a short summary of what you implemented.
